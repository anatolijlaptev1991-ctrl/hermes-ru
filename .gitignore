node_modules/
*.log
.hermes-ru-patched
app.asar.orig
# Большой JS-бандл доставляется через GitHub Releases (см. dist/ в релизе),
# чтобы не перегружать git-репозиторий.
dist/assets/index-*.js
dist/
# Release artifact (uploaded to GitHub Releases, not in git)
hermes-ru-dist-*.zip
