node_modules/
*.log
.hermes-ru-patched
app.asar.orig
# Release artifact (uploaded to GitHub Releases, not in git)
hermes-ru-dist-*.zip
# dist/ and config/ are NOT needed in git (defineLocale+build method)
# dist/ is generated by npm run build in Hermes source
# config/ was used by old in-place method (removed)
dist/
config/
# npm pack build artifact (not in repo)
hermes-ru-*.tgz
